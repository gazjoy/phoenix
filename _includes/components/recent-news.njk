<section>

	<h2>Recent News</h2>

	{% for newsPost in news | reverse | take(2) %}

		<!--{{ newsPost | dump }}-->

		<h3>
			<a href="/news/{{newsPost.slug}}">
					{{ newsPost.title }}
			</a>
		</h3>
			
		{{ newsPost.bodyRichText | renderRichTextAsHtml | safe }}
			
		<p>
			<small>{{ newsPost.date | readableDate }} by {{ newsPost.authorName }}</small>
		</p>

	{% endfor %}
</section>
